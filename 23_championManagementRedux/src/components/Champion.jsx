import { useState } from "react"
import { useSelector, useDispatch } from "react-redux"
import { add, update, remove } from "../store/championSlice"

export default function Champion() {
  return (
    <div>
        <h2>Champion Management</h2>
        
    </div>
  )
}
